# این خطوط رو به انتهای requirements.txt فعلی پروژه (تو ریشه‌ی ریپازیتوری) اضافه کن.
# فایل‌های قبلی (requests, pandas, numpy یا هرچی از قبل بود) رو پاک نکن، فقط این‌ها رو اضافه کن.

feedparser
--extra-index-url https://download.pytorch.org/whl/cpu
torch
transformers

# اگه به‌جای FinBERT از پلن سبک (VADER) استفاده می‌کنی (analyzer/news_sentiment.py,
# USE_LIGHTWEIGHT_FALLBACK = True)، به‌جای سه خط بالا (torch/transformers) این یکی رو بذار:
# vaderSentiment
